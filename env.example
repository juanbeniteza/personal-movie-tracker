# Neon (or Supabase) PostgreSQL connection string — must include ?sslmode=require
DATABASE_URL=""

# TMDB v3 API key — from https://www.themoviedb.org/settings/api
TMDB_API_KEY=""

# Static Bearer token for the POST /api/movies endpoint (use a long random string)
API_ROUTE_TOKEN=""

ADD_PASSWORD=""